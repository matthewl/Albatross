<% if @website.display_banner? %>
  <section class="p-4 text-center">
    <div class="flex items-center rounded py-2 px-8 leading-none lg:inline-flex <%= @light == true ? "bg-emerald-100" : "bg-emerald-800" %> <%= @light == true ? "text-emerald-800" : "text-emerald-100" %>">
      <span class="flex-auto font-semibold leading-loose text-sm">
        <%= @website.banner_text %>
      </span>
    </div>
  </section>
<% end %>