<% if @website.display_banner? %>
  <section class="p-4 text-center">
    <div class="flex items-center rounded-full bg-amber-100 py-2 px-8 leading-none text-amber-800 lg:inline-flex">
      <span class="flex-auto font-semibold leading-loose text-sm">
        <%= @website.banner_text %>
      </span>
    </div>
  </section>
<% end %>