<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="<%= @website.name %>">
    <meta name="author" content="<%= @website.name %>">
    <title>Welcome to <%= @website.name %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-green-50 parkland">
    <header>
      <section class="w-full h-screen relative">
        <%= image_tag "sabrehill-header-background.jpg", class: "object-cover w-full h-full" %>

        <div class="absolute mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 top-0 left-0">
          <nav class="relative z-50 flex justify-between">
            <div class="flex items-center md:gap-x-12 -mt-8">
              <a aria-label="Home" href="/#">
                <%= image_tag "sabrehill-logo.png", class: "w-48" %>
              </a>

              <div class="hidden md:flex md:gap-x-6">
                <a class="inline-block rounded-lg py-1 px-2 font-bold hover:bg-slate-100 hover:text-slate-900" href="/#home">Home</a>
                <a class="inline-block rounded-lg py-1 px-2 font-bold hover:bg-slate-100 hover:text-slate-900" href="/#clubhouse">Clubhouse</a>
                <a class="inline-block rounded-lg py-1 px-2 font-bold hover:bg-slate-100 hover:text-slate-900" href="/#course">Course</a>
                <a class="inline-block rounded-lg py-1 px-2 font-bold hover:bg-slate-100 hover:text-slate-900" href="/#course">Contact</a>
              </div>
            </div>
          </nav>
        </div>
      </section>
    </header>
    <%= yield %>
  </body>
</html>
