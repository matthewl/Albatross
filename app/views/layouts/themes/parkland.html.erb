<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="<%= @current_website.name %>">
    <meta name="author" content="<%= @current_website.name %>">
    <title>Welcome to <%= @current_website.name %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <link href="https://fonts.googleapis.com/css2?family=<%= Themes::Parkland.body_font_family %>:ital,wght@0,400;1,700&family=<%= Themes::Parkland.headings_font_family %>:ital,wght@0,700;1,400&display=swap" rel="stylesheet">
    <style>
      body {
        font-family: "<%= Themes::Parkland::BODY_FONT_FAMILY %>", Roboto, Helvetica, Arial, sans-serif;
      }

      h1, h2, h3, h4, h5, h6, p.heading, label.heading, nav {
        font-family: "<%= Themes::Parkland::HEADINGS_FONT_FAMILY %>", Roboto, Helvetica, Arial, sans-serif;
      }
    </style>
  </head>

  <body class="parkland">
    <header>
      <!-- TODO: Decide on the best place for this.
      <div class="w-full px-4 bg-green-700">
        <nav class="relative z-50 flex justify-between px-4 py-2">
          <div class="flex items-center md:gap-x-12">
            <a aria-label="Home" href="/#">
              <%= image_tag "sabrehill-logo.png", class: "w-48" %>
            </a>

            <div class="hidden md:flex md:gap-x-6">
              <a class="inline-block px-2 py-1 text-xl font-bold text-white rounded-lg" href="/#home">Home</a>
              <a class="inline-block px-2 py-1 text-xl font-bold text-white rounded-lg" href="/#clubhouse">Clubhouse</a>
              <a class="inline-block px-2 py-1 text-xl font-bold text-white rounded-lg" href="/#course">Course</a>
              <a class="inline-block px-2 py-1 text-xl font-bold text-white rounded-lg" href="/#course">Contact</a>
            </div>
          </div>
        </nav>
      </div>
      -->
      <%= render(Themes::Parkland::HeaderComponent.new(header: @current_website.headers.active_header)) do |c| %>
        <%= c.with_banner(website: @current_website)%>
      <% end %>
    </header>

    <main>
      <%= render "shared/flash_notification" %>
      <%= yield %>
    </main>

    <%= render(Themes::Links::FooterComponent.new(website: @current_website)) %>
  </body>
</html>
